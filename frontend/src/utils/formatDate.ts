// What: Pure helper functions — no React, no components, just logic.

// utils/
// ├── formatDate.ts       ← format dates consistently
// ├── formatCurrency.ts   ← format peso amounts
// └── axiosInstance.ts    ← base Axios config with token injection